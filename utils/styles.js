import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  form: {
    borderWidth: 1,
    borderColor: '#bae8e8',
    borderRadius: 5,
    borderStyle: 'dashed',
    padding: 16,
    marginTop: 16,
    marginBottom: 16,
  },
});

export default styles;
